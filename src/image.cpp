#include "image.h"
#include "load.h"
#include "save.h"

namespace kalam {
bool Image::load(const std::string &filepath) {
  // we will implement loading image according to the file extension
  return true;
}

bool Image::save(const std::string &filepath) {
  // we will implement saving image according to the file extension
  return true;
}
} // namespace kalam
